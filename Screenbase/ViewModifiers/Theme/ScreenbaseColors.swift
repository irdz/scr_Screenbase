//
//  ScreenbaseColors.swift
//  Screenbase
//

import SwiftUI

/// Design tokens from PRD §24, backed by asset catalog colors with light/dark variants.
enum ScreenbaseColors {
    // MARK: Product chrome

    static let background = Color("Background")
    static let ink = Color("Ink")
    static let lightGray = Color("LightGray")
    static let gray = Color("Gray")
    static let navInactive = Color("NavInactive")
    static let elevated = Color("Elevated")
    static let line = Color("Line")
    static let lineCool = Color("LineCool")
    static let red = Color("Red")
    static let green = Color("Green")

    // MARK: Brand / marketing (icon & marketing surfaces)

    static let navy = Color("Navy")
    static let navyDeep = Color("NavyDeep")
    static let navyPanel = Color("NavyPanel")
    static let sky = Color("Sky")
    static let skyLight = Color("SkyLight")
    static let amber = Color("Amber")
}
